# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Either of these is fine for server-side service client
SUPABASE_SERVICE_ROLE=
SUPABASE_SERVICE_ROLE_KEY=

# Public envs (expose in client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_NAME=DigiClient

# SMTP for email notifications (server-side only)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="DigiClient <no-reply@yourdomain.tld>"
ALLOW_DEV_SEED_FOR_VIEWER=true